<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.invoiceapp_josephambayec" >

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat" >
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".NewCustomerActivity"
            android:label="New Customer" />
        <activity
            android:name=".NewAddressActivity"
            android:label="New Address" />
        <activity
            android:name=".EditCustomerActivity"
            android:label="Edit Customer" />
        <activity
            android:name=".ShowInvoicesActivity"
            android:label="Invoices" />
        <activity
            android:name=".EditInvoiceActivity"
            android:label="Edit Invoice" />
        <activity
            android:name=".ShowInvoiceActivity"
            android:label="Invoice" />
        <activity
            android:name=".EditInvoiceDetailActivity"
            android:label="Update Invoice Detail" />
        <activity
            android:name=".NewInvoiceDetailActivity"
            android:label="New Invoice Detail" />
        <activity
            android:name=".SettingsActivity"
            android:label="Settings" />
    </application>

</manifest>